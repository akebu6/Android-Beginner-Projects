name: 'Label Actions'

on:
  issues:
    types: [unlabeled, labeled]
  pull_request:
    types: [unlabeled, labeled]
  discussion:
    types: [unlabeled, labeled]

permissions:
  contents: read
  issues: write
  pull-requests: write
  discussions: write

jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: dessant/label-actions@v2
